import {View, Text, ImageBackground} from 'react-native';
import React from 'react';
import {BlogImage} from '../../assets';

const GTWhatOurClient = () => {
  return (
    <ImageBackground source={BlogImage}>
      <Text>GTWhatOurClient</Text>
    </ImageBackground>
  );
};

export default GTWhatOurClient;
